# Report Templates

Phase 1 reports are rendered by `report_generator.py`.

The current template structure is intentionally code-backed so tests can verify
required sections, labels, disclaimers, and prohibited wording consistently.
